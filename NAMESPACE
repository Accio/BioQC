exportMethods("wmwTest")
exportClasses(gsitem, gslist)
exportMethods("gsName", "gsDesc", "gsGenes", "gsUniqGenes",
              "gsWeights","gsHasWeights", "gsClearWeights",
              "gsIndices")
exportMethods("gsName<-", "gsDesc<-", "gsGenes<-",
              "gsWeights<-", "gsIndices<-")
exportMethods("gsitem")

export(as.data.frame.gslist, as.matrix.gslist)
export(gsApply, tfidf, gsTfIdf)

export(gini)

export(absLog10p)

export(filterPmat)

export(readGmt)

export(entropy,
       entropyDiversity,
       entropySpecificity,
       sampleSpecialization)

useDynLib(BioQC)
